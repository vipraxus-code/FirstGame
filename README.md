# **IN WORK**
This repository is still a work in progress, so many things may change, including this file.

## **First game (pygame)**
A small lerning project written in Python using the pygame library.
You control a spaceship, move around the screen and destroy alien spaceships.

### **Requirements**
- Python 3.14
  - pygame library (install with "pip install pygame")