# IN WORK

## First game (pygame)
A small lerning project written in Python using the pygame library.
You control a spaceship, move around the screen and shoot while a fleet of aliens is generated.

### Requirements
- Python
- pygame library (install with "pip install pygame")